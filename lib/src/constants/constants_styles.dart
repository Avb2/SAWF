// Styles
  import 'package:flutter/material.dart';

import 'constants_colors.dart';

var randomStockTextStyle = const TextStyle(
      fontWeight: FontWeight.bold,
      fontSize: 20,
      color: NormalTheme.secondaryColor
  );
  var stockInputFieldTextStyle = const TextStyle(
      fontSize: 20,
      color: Colors.white60
  );
  var enterButtonTextStyle = const TextStyle(
      fontSize: 20,
      color: NormalTheme.primaryColor
  );

    var randomStockBoxDeco= BoxDecoration(
      border: Border.all(
          color: Colors.black,
          width: 2)
  );